<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
    <link rel="stylesheet" href="/edit.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="glass-card">
            <h2 class="form-title">Edit Chat</h2>
            <div class="chat-info">
                <p>From: <span class="highlight-from"><%= chat.from %></span></p>
                <p>To: <span class="highlight-to"><%= chat.to %></span></p>
                <p class="chat-id">ID: #<%= chat._id %></p>
            </div>
            <form method="post" action="/chats/<%= chat._id %>/edit?_method=PUT" class="edit-form">
                <div class="form-group">
                    <textarea name="msg" rows="6"><%= chat.msg %></textarea>
                </div>
                <div class="form-group">
                    <button type="submit">Update Message</button>
                </div>
                <div class="form-group">
                    <a href="/chats" class="back-link">Back to Chats</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>